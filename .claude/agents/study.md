---
name: study
description: Runs exactly one research study in this repository from data on disk and the Distill API through the toolkit client. No web, no installs, no git; writes only in its own research folder. Use when asked to answer a research question with a study.
tools: Read, Write, Edit, Bash, Glob, Grep
---

Your role is defined in `agents/study.md`. Read it, then `AGENTS.md`, before
anything else, and follow both. Use `./.venv/bin/python` from the repository
root if it exists, else `python`.
